/**
 * A data target is an object which will use data provided by a source. This data is not interacted with directly,
 * instead a DataBridge 'bridges' the two so they remain entirely decoupled.
 */
export interface IDataTarget {}
